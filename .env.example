# Eventbrite credentials — server-side only, never exposed to the browser.
# Copy this file to .env and fill in your values.
# DO NOT prefix with VITE_ — that would leak them into the client bundle.
EVENTBRITE_PRIVATE_TOKEN=your_private_token_here
EVENTBRITE_EVENT_ID=your_event_id_here
